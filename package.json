{
  "name": "ai-job-finder",
  "version": "1.0.0",
  "description": "AI-powered job finder with Claude + web search",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "build": "cd client && npm install && npm run build",
    "dev": "concurrently \"node server/index.js\" \"cd client && npm start\""
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.24.0",
    "concurrently": "^8.2.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "openai": "^6.32.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
